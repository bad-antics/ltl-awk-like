// examples/demo.ltl — runnable demo for ltl_awk_like

import std::io
use ltl_awk_like as lib

fn main() {
    io::println("ltl_awk_like demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}