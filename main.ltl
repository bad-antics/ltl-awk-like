// ltl-awk-like/main.ltl
//
// Pattern-action data processor for tabular text.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-awk-like v0.1.0")
    io::println("Pattern-action data processor for tabular text.")
    0
}
